{
  "name": "mineria-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../dots-go && npm run build && @powershell Copy-Item dist -Recurse ../mineriaBackend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "mongoose": "^8.8.0",
    "socket.io": "^4.8.1"
  }
}
